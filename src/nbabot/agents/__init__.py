"""Game-phase and research/execution agents."""

from . import (  # noqa: F401
    autopilot,
    backtest,
    baseline,
    book_watch,
    daily_cycle,
    demo_execute,
    discover_markets,
    heartbeat,
    lineups,
    live_execute,
    lock,
    market_matcher,
    paper,
    ports,
    portfolio_sync,
    reconcile,
    research_agent,
    slate_discovery,
    slate_verify,
    snapshot_market,
    source_check,
    status,
    telegram_test,
    ui,
)

PHASES = {
    "autopilot": autopilot.run,
    "backtest": backtest.run,
    "baseline": baseline.run,
    "book-watch": book_watch.run,
    "daily-cycle": daily_cycle.run,
    "demo-execute": demo_execute.run,
    "discover-markets": discover_markets.run,
    "lineups": lineups.run,
    "live-execute": live_execute.run,
    "lock": lock.run,
    "heartbeat": heartbeat.run,
    "live": heartbeat.run,      # alias so a plain crontab can drive the live loop
    "market-matcher": market_matcher.run,
    "paper": paper.run,
    "ports": ports.run,
    "portfolio-sync": portfolio_sync.run,
    "reconcile": reconcile.run,
    "research-agent": research_agent.run,
    "slate-discovery": slate_discovery.run,
    "slate-verify": slate_verify.run,
    "snapshot-market": snapshot_market.run,
    "source-check": source_check.run,
    "status": status.run,
    "telegram-test": telegram_test.run,
    "ui": ui.run,
}
